name = "bug_fixer"
description = "Applies the verified implementation plan, runs tests, and writes the fix summary."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Work only in homework-4.
Read homework-4/agents/bug-fixer.agent.md and the complete implementation plan.
Use test-first changes, run focused tests after each edit, and stop on failure.
Never reintroduce a seeded issue when the after-state is already satisfied.
Write context/bugs/001-order-receipt/fix-summary.md.
"""
